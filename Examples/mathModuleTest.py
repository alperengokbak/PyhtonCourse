import mathsModule
# We can use different name this pyhton class, we can show that in example:
import mathsModule as mM
# If we want to take just one "def" or "Properties" we can use that form:
from mathsModule import addition


#mathsModule.addition(5 , 10)
mM.addition(10 , 5)
print(mM.customer["firstName"])